/* Frontend configuration.
 *
 * RL_API: the base URL of your deployed Render backend, e.g.
 *   window.RL_API = "https://rl-trader-api.onrender.com";
 *
 * Leave it as an empty string to run the dashboard FULLY STATIC — it ships a
 * baked results.js, so the site works with no backend at all. Set RL_API to
 * light up the optional "Run live" inference widget. */
window.RL_API = "";
