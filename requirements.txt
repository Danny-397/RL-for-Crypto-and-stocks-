# Deployment / runtime dependencies — deliberately LIGHTWEIGHT and torch-free.
#
# This is what a host (Render, etc.) installs to *serve* the trained policy: the
# backend runs the network as plain NumPy matmuls, so it needs no PyTorch/CUDA.
# Keeping torch out of here means the API container builds in ~30s instead of
# pulling a ~2GB CUDA stack it never uses.
#
#   pip install -r requirements.txt      # runtime / serving (this file)
#   pip install -e .                     # FULL dev/training env (adds torch,
#                                        #   matplotlib, etc. — see pyproject.toml)
numpy>=1.23
pandas>=1.5
gymnasium>=0.29

# --- API server deps ---------------------------------------------------------
flask>=3.0
flask-cors>=4.0
gunicorn>=21.0
yfinance>=1.4
curl_cffi>=0.6
